import chalk from "chalk";
import logSymbols from "log-symbols";

console.log(chalk.bgBlue("Testet funcionando"));
console.log(chalk.green.bold("Servidor iniciado"));
console.log(chalk.red.bold("Falha ao iniciar o servidor"));
